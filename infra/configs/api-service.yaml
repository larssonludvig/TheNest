apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: database
spec:
  selector:
    app: api
  type: LoadBalancer
  ports:
    - name: api-port
      protocol: TCP
      port: 90
      targetPort: 80
